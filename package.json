{
  "name": "project-slowlife-breakout",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "build": "vite build",
    "test": "node --test",
    "start": "node server/index.js",
    "preview": "npm run build && node server/index.js --production"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  }
}
